#pragma once

#ifdef GAMEQC
// boss states synced with the client
const int BOSS_FROZEN = BIT(0); // boss is stunned (.is_frozen)
const int BOSS_AGGRO = BIT(1); // boss has been aggro'd (had .enemy set at some point)

.bool had_enemy; // stored state to tell if the boss had an enemy

IntrusiveList g_bossbars;
STATIC_INIT(BossBars_List)
{
    g_bossbars = IL_NEW();
}
#endif

#ifdef SVQC
void bossbar_spawn(entity this, Monster mon);
#endif

#ifdef CSQC
void HUD_BossBars();
#endif
